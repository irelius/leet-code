import { describe, test, expect } from "vitest";
import { symmetricTree } from "../questions/101-symmetric-tree";

describe("101 - Symmetric Tree", () => {
    test("Case 1", () => {
        expect(symmetricTree()).toEqual()
    })
    
    test("Case 2", () => {
        expect(symmetricTree()).toEqual()
    })

    test("Case 3", () => {
        expect(symmetricTree()).toEqual()
    })
})
        